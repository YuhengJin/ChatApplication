Pour tester :

On lance le fichier Graphique.java,  (dans package Interface)

1)  Le login page marche tres bien, une fois on connecte,on broadcast a tout le monde, les user deja connecte a recu les message, et l’etudier, comme ca on recupere les ports, et distinguer si c’est deja dans la liste connecte, sinon on ajoute dans la liste. Et on fait mise a jour a l’interface graphique


2) on a reussi a mettre a jour la liste des users connectees grace a broadcast, mais on a pas tout fini la partie l’affichage de message recu.
 Le groupe chat on a reussi a coder la plupart( avec nulticast), mais on a pas 100% fini

3) Le JDBC, on n’a pas encore travaille sur ca.





Pour la partie TCP et UDP, on a deux tests independent

dans le     MainTest.java     (dans package Application)

La premiere partie sert a communiquer avec TCP (Pour le moment on a mis  en commentaire)

Deuxieme partie sert a tester le multicast et broadcast avec UDP

Tout marche tres bien 
